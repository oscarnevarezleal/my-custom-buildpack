laraboot-commander:
  commands:
    - name: Init
      run: |
        echo "Init"
        pwd
    - name: NoOps
      run: |
        echo "Hello"
        ls -ltah
    - name: Install deps
      run: |
        echo "installing"